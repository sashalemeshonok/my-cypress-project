# название workflow
name: Runing tests manual
# на какой триггер/событие workflow будет реагировать
on: workflow_dispatch
#
jobs:
  #имя "работы" которая будет запускаться при отработке события on:
  manualRunTest:
    runs-on: ubuntu-latest
    steps:
      - name: print to console
        run: echo Hello GH Actions!
